pipeline {
    agent any
    
    stages {
        stage('Build Docker image') {
            steps {
                sh "/usr/local/bin/docker duild -t image ."
            }
        }
        
        stage('Cleanup') {
            steps {
                sh '/usr/local/bin/docker rmi lab3:latest'
            }
        }
    }
}
