pipeline {
    agent any
    
    stages {
        stage('Build Docker image') {
            steps {
                sh "/usr/local/bin/docker compose up"
            }
        }
        
        stage('Cleanup') {
            steps {
                sh '/usr/local/bin/docker rmi lab3:latest'
            }
        }
    }
}
